# 01 Context Memory

This topic covers context engineering, memory, RAG, retrieval, knowledge-base design, progressive disclosure, and long-term personalization.

Use it when the question is about what an agent should know, remember, retrieve, compress, cite, or carry across sessions.

Default path:

1. Search with `.\kb.ps1 search "<query>" --topic 01-context-memory`.
2. Open matched `summary.md`.
3. Open `article.md` only for evidence or deep reading.
