# 02 Tools Actions

This topic covers tools, browser automation, OpenCLI, CLI adapters, APIs, MCP-like tool surfaces, sandboxes, coding agents, and execution environments.

Use it when the question is about how agents act in the world, call tools, operate browsers, run code, or integrate with external systems.

Default path:

1. Search with `.\kb.ps1 search "<query>" --topic 02-tools-actions`.
2. Open matched `summary.md`.
3. Open `article.md` only for evidence or deep reading.
