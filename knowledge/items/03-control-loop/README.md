# 03 Control Loop

This topic covers planning, ReAct-style loops, orchestration, workflows, multi-agent coordination, harness engineering, and task completion behavior.

Use it when the question is about how an agent decides the next step, tracks state, handles long tasks, coordinates subagents, or knows when it is done.

Default path:

1. Search with `.\kb.ps1 search "<query>" --topic 03-control-loop`.
2. Open matched `summary.md`.
3. Open `article.md` only for evidence or deep reading.
